---
interface InstagramPost {
  id: string;
  media_type: 'IMAGE' | 'VIDEO' | 'CAROUSEL_ALBUM';
  media_url: string;
  thumbnail_url?: string;
  caption?: string;
  permalink: string;
  timestamp: string;
}

interface Props {
  limit?: number;
  showInFooter?: boolean;
}

const { limit = 8, showInFooter = true } = Astro.props;

// Instagram API integration
async function fetchInstagramPosts(): Promise<InstagramPost[]> {
  // Check if we have Instagram credentials
  const accessToken = import.meta.env.INSTAGRAM_ACCESS_TOKEN;
  const appId = import.meta.env.INSTAGRAM_APP_ID;

  if (!accessToken) {
    console.warn('Instagram access token not found. Using placeholder data.');
    return []; // Return empty array if no token
  }

  try {
    const response = await fetch(
      `https://graph.instagram.com/me/media?fields=id,media_type,media_url,thumbnail_url,caption,permalink,timestamp&access_token=${accessToken}&limit=${limit}`
    );

    if (!response.ok) {
      throw new Error(`Instagram API error: ${response.status}`);
    }

    const data = await response.json();
    return data.data || [];
  } catch (error) {
    console.error('Failed to fetch Instagram posts:', error);
    return []; // Return empty array on error
  }
}

const posts = await fetchInstagramPosts();
---

{posts.length > 0 && (
  <section class={showInFooter ? "bg-gray-50 py-12" : "py-8"}>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <!-- Header -->
      <div class="text-center mb-8">
        <h3 class="text-2xl font-bold text-gray-900 mb-2">
          Follow Our Rescues
        </h3>
        <p class="text-gray-600 mb-4">
          See our wonderful animals on their journey to forever homes
        </p>
        <a
          href="https://instagram.com/homelesshounds"
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center text-indigo-600 hover:text-indigo-700 font-medium transition-colors duration-200"
        >
          <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12.017 0C5.396 0 .029 5.367.029 11.987c0 6.62 5.367 11.987 11.988 11.987s11.987-5.367 11.987-11.987C24.004 5.367 18.637.001 12.017.001zM8.449 16.988c-1.297 0-2.348-1.051-2.348-2.348s1.051-2.348 2.348-2.348 2.348 1.051 2.348 2.348-1.051 2.348-2.348 2.348zm7.718 0c-1.297 0-2.348-1.051-2.348-2.348s1.051-2.348 2.348-2.348 2.348 1.051 2.348 2.348-1.051 2.348-2.348 2.348z"/>
          </svg>
          @homelesshounds
        </a>
      </div>

      <!-- Instagram Grid -->
      <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-4 gap-4">
        {posts.map((post) => (
          <a
            href={post.permalink}
            target="_blank"
            rel="noopener noreferrer"
            class="group relative aspect-square overflow-hidden rounded-lg bg-gray-200 hover:opacity-90 transition-opacity duration-200"
          >
            <img
              src={post.media_type === 'VIDEO' ? post.thumbnail_url : post.media_url}
              alt={post.caption ? post.caption.substring(0, 100) + '...' : 'Instagram post'}
              class="w-full h-full object-cover transition-transform duration-300 group-hover:scale-105"
              loading="lazy"
              decoding="async"
            />

            <!-- Video indicator -->
            {post.media_type === 'VIDEO' && (
              <div class="absolute inset-0 flex items-center justify-center">
                <div class="bg-black/50 rounded-full p-2">
                  <svg class="w-6 h-6 text-white" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M8 5v14l11-7z"/>
                  </svg>
                </div>
              </div>
            )}

            <!-- Hover overlay -->
            <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300">
              <div class="absolute bottom-2 left-2 right-2">
                {post.caption && (
                  <p class="text-white text-xs line-clamp-2 leading-tight">
                    {post.caption.substring(0, 80)}...
                  </p>
                )}
              </div>
            </div>
          </a>
        ))}
      </div>

      <!-- View More Link -->
      {posts.length >= limit && (
        <div class="text-center mt-8">
          <a
            href="https://instagram.com/homelesshounds"
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center px-6 py-3 border border-indigo-600 text-indigo-600 hover:bg-indigo-600 hover:text-white font-medium rounded-lg transition-colors duration-200"
          >
            View More on Instagram
            <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
            </svg>
          </a>
        </div>
      )}
    </div>
  </section>
)}

<!-- Fallback for no posts/no token -->
{posts.length === 0 && (
  <section class={showInFooter ? "bg-gray-50 py-8" : "py-6"}>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h3 class="text-xl font-bold text-gray-900 mb-2">
        Follow Our Rescues
      </h3>
      <p class="text-gray-600 mb-4">
        See our wonderful animals on Instagram
      </p>
      <a
        href="https://instagram.com/homelesshounds"
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center text-indigo-600 hover:text-indigo-700 font-medium transition-colors duration-200"
      >
        <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 24 24">
          <path d="M12.017 0C5.396 0 .029 5.367.029 11.987c0 6.62 5.367 11.987 11.988 11.987s11.987-5.367 11.987-11.987C24.004 5.367 18.637.001 12.017.001zM8.449 16.988c-1.297 0-2.348-1.051-2.348-2.348s1.051-2.348 2.348-2.348 2.348 1.051 2.348 2.348-1.051 2.348-2.348 2.348zm7.718 0c-1.297 0-2.348-1.051-2.348-2.348s1.051-2.348 2.348-2.348 2.348 1.051 2.348 2.348-1.051 2.348-2.348 2.348z"/>
        </svg>
        @homelesshounds
      </a>
    </div>
  </section>
)}