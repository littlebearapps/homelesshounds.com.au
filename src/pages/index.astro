---
export const prerender = false;

import Base from "../layouts/Base.astro";
import AnimalCard from "../components/AnimalCard.astro";
import type { AdoptableAnimalsResponse } from "../types/asm";

// Fetch adoptable animals from our API
let animalsData: AdoptableAnimalsResponse | null = null;
let error: string | null = null;

try {
  const response = await fetch(`${Astro.url.origin}/api/asm/adoptable?species=all&limit=8`);
  if (!response.ok) {
    throw new Error(`API returned ${response.status}`);
  }
  animalsData = await response.json();
} catch (e) {
  console.error('Error fetching animals:', e);
  error = e instanceof Error ? e.message : 'Failed to fetch animals';
}

const animals = animalsData?.animals || [];

// Statistics data
const stats = {
  animalsRescued: '400+',
  activeFosters: '50+',
  adoptions: '300+',
  volunteers: '100+'
};

// Real partner names from WordPress site
const partners = [
  'Toorak Road Veterinary Clinic',
  'Heathmont Animal Hospital',
  'Pet Circle',
  'Pro Dog Training',
  'Pointon Partners Lawyers'
];
---

<Base title="Homeless Hounds Animal Rescue | Rescue Dogs and Cats Love You More">
  <main>
    <!-- Hero Section - Full Width -->
    <section class="relative min-h-[80vh] bg-gradient-to-br from-purple-900 via-purple-700 to-pink-600 flex items-center">
      <div class="absolute inset-0 bg-black opacity-20"></div>
      <div class="relative w-full max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
        <div class="text-center">
          <h1 class="text-5xl md:text-7xl font-bold text-white mb-6 leading-tight">
            Find Your<br />
            <span class="text-yellow-400">Forever Friend</span>
          </h1>
          <p class="text-xl md:text-2xl text-white/90 mb-10 max-w-3xl mx-auto">
            Homeless Hounds rescues, rehabilitates and rehomes dogs, cats and other animals in need across Victoria
          </p>
          <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a href="/adopt" class="inline-block bg-yellow-400 text-purple-900 font-bold py-4 px-10 rounded-full text-lg hover:bg-yellow-300 transform hover:scale-105 transition-all shadow-xl">
              View Adoptable Pets
            </a>
            <a href="https://donate.homelesshounds.com.au" target="_blank" class="inline-block bg-white text-purple-700 font-bold py-4 px-10 rounded-full text-lg hover:bg-gray-100 transform hover:scale-105 transition-all shadow-xl">
              Donate Now
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- Mission Cards - Full Width Background -->
    <section class="relative py-20 bg-gradient-to-b from-gray-50 to-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
          <!-- Adopt Card -->
          <div class="group relative overflow-hidden rounded-2xl bg-white shadow-xl hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2">
            <div class="aspect-w-16 aspect-h-12 bg-gradient-to-br from-purple-400 to-purple-600 p-8 text-center">
              <div class="text-6xl mb-4">🏠</div>
              <h3 class="text-2xl font-bold text-white">Adopt</h3>
            </div>
            <div class="p-6">
              <p class="text-gray-600 mb-4">
                Visit with our rescue animals ready for adoption. Find your perfect match today!
              </p>
              <a href="/adopt" class="inline-flex items-center text-purple-600 font-semibold hover:text-purple-700">
                Learn More
                <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </a>
            </div>
          </div>

          <!-- Foster Card -->
          <div class="group relative overflow-hidden rounded-2xl bg-white shadow-xl hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2">
            <div class="aspect-w-16 aspect-h-12 bg-gradient-to-br from-blue-400 to-blue-600 p-8 text-center">
              <div class="text-6xl mb-4">💝</div>
              <h3 class="text-2xl font-bold text-white">Foster</h3>
            </div>
            <div class="p-6">
              <p class="text-gray-600 mb-4">
                Without fostering, there can be no rescue. Help us save over 400 animals yearly.
              </p>
              <a href="/get-involved/foster-care" class="inline-flex items-center text-blue-600 font-semibold hover:text-blue-700">
                Learn More
                <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </a>
            </div>
          </div>

          <!-- Donate Card -->
          <div class="group relative overflow-hidden rounded-2xl bg-white shadow-xl hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2">
            <div class="aspect-w-16 aspect-h-12 bg-gradient-to-br from-green-400 to-green-600 p-8 text-center">
              <div class="text-6xl mb-4">💚</div>
              <h3 class="text-2xl font-bold text-white">Donate</h3>
            </div>
            <div class="p-6">
              <p class="text-gray-600 mb-4">
                Every little bit counts! Your generous donation helps animals most in need.
              </p>
              <a href="https://donate.homelesshounds.com.au" target="_blank" class="inline-flex items-center text-green-600 font-semibold hover:text-green-700">
                Learn More
                <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </a>
            </div>
          </div>

          <!-- Volunteer Card -->
          <div class="group relative overflow-hidden rounded-2xl bg-white shadow-xl hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-2">
            <div class="aspect-w-16 aspect-h-12 bg-gradient-to-br from-orange-400 to-orange-600 p-8 text-center">
              <div class="text-6xl mb-4">🤝</div>
              <h3 class="text-2xl font-bold text-white">Volunteer</h3>
            </div>
            <div class="p-6">
              <p class="text-gray-600 mb-4">
                Join our dedicated team of volunteers in a variety of roles that help save lives.
              </p>
              <a href="/get-involved/volunteer" class="inline-flex items-center text-orange-600 font-semibold hover:text-orange-700">
                Learn More
                <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
              </a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Looking to Adopt Section -->
    <section class="py-20 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
          <h2 class="text-4xl font-bold text-gray-900 mb-4">Looking to Adopt?</h2>
          <p class="text-xl text-gray-600">View all of our rescue animals that are ready for adoption!</p>
        </div>

        {animals.length > 0 ? (
          <div class="adoption-carousel relative">
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6" id="adoption-grid">
              {animals.slice(0, 8).map((animal) => {
                // Construct the image URL using the ASM media endpoint
                const imageUrl = animal.WEBSITEIMAGECOUNT > 0 && animal.WEBSITEMEDIANAME
                  ? `https://service.sheltermanager.com/asmservice?method=animal_image&account=st3418&animalid=${animal.ID}`
                  : '/hh-logo.png';

                return (
                  <div class="group relative overflow-hidden rounded-xl bg-white shadow-lg hover:shadow-2xl transition-all duration-300 transform hover:-translate-y-1">
                    <div class="aspect-w-4 aspect-h-3 relative overflow-hidden bg-gray-100" style="height: 280px">
                      <img
                        src={imageUrl}
                        alt={animal.ANIMALNAME}
                        class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-300"
                        loading="lazy"
                      />
                      {animal.SPECIESNAME && (
                        <span class="absolute top-3 right-3 bg-white/90 backdrop-blur text-purple-700 text-xs font-bold px-3 py-1 rounded-full shadow-md">
                          {animal.SPECIESNAME}
                        </span>
                      )}
                    </div>
                    <div class="p-4">
                      <h3 class="text-xl font-bold text-gray-900 mb-1">{animal.ANIMALNAME}</h3>
                      <p class="text-sm text-gray-600 mb-3">
                        {animal.BREEDNAME || 'Unknown Breed'} • {animal.AGEGROUP || 'Unknown Age'} • {animal.SEXNAME || 'Unknown'}
                      </p>
                      <a
                        href={`/pets/${animal.SPECIESNAME?.toLowerCase() || 'other'}/${animal.ID}`}
                        class="inline-flex items-center text-purple-600 font-semibold hover:text-purple-700"
                      >
                        Meet {animal.ANIMALNAME}
                        <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                        </svg>
                      </a>
                    </div>
                  </div>
                );
              })}
            </div>
          </div>
        ) : (
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
            {[1, 2, 3, 4].map(() => (
              <div class="bg-white rounded-xl shadow-lg overflow-hidden">
                <div class="h-64 bg-gray-200 animate-pulse"></div>
                <div class="p-4">
                  <div class="h-6 bg-gray-200 rounded animate-pulse mb-2"></div>
                  <div class="h-4 bg-gray-200 rounded animate-pulse w-3/4"></div>
                </div>
              </div>
            ))}
          </div>
        )}

        <div class="mt-12 text-center">
          <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
            <span class="text-lg font-semibold text-gray-700">Meet:</span>
            <div class="flex flex-wrap gap-3 justify-center">
              <a href="/adopt" class="inline-flex items-center bg-purple-600 text-white font-bold py-3 px-6 rounded-full hover:bg-purple-700 transform hover:scale-105 transition-all shadow-lg">
                <span class="mr-2">🏠</span> All Animals
              </a>
              <a href="/adopt/dogs" class="inline-flex items-center bg-blue-600 text-white font-bold py-3 px-6 rounded-full hover:bg-blue-700 transform hover:scale-105 transition-all shadow-lg">
                <span class="mr-2">🐕</span> Dogs
              </a>
              <a href="/adopt/cats" class="inline-flex items-center bg-pink-600 text-white font-bold py-3 px-6 rounded-full hover:bg-pink-700 transform hover:scale-105 transition-all shadow-lg">
                <span class="mr-2">🐱</span> Cats
              </a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Statistics Section -->
    <section class="py-20 bg-gradient-to-r from-purple-600 to-pink-600 text-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 class="text-4xl font-bold text-center mb-12">Our Impact</h2>
        <div class="grid md:grid-cols-4 gap-8">
          <div class="text-center">
            <div class="text-5xl font-bold mb-2">{stats.animalsRescued}</div>
            <p class="text-xl opacity-90">Animals Rescued Yearly</p>
          </div>
          <div class="text-center">
            <div class="text-5xl font-bold mb-2">{stats.activeFosters}</div>
            <p class="text-xl opacity-90">Active Foster Carers</p>
          </div>
          <div class="text-center">
            <div class="text-5xl font-bold mb-2">{stats.adoptions}</div>
            <p class="text-xl opacity-90">Successful Adoptions</p>
          </div>
          <div class="text-center">
            <div class="text-5xl font-bold mb-2">{stats.volunteers}</div>
            <p class="text-xl opacity-90">Dedicated Volunteers</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Partners Section -->
    <section class="py-20 bg-gray-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h3 class="text-3xl font-bold text-center text-gray-900 mb-12">
          Thank you to our partners whose support makes our work possible
        </h3>

        <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-5 gap-8">
          {partners.map((partner) => (
            <div class="bg-white rounded-xl p-6 shadow-md hover:shadow-xl transition-all duration-300 flex items-center justify-center min-h-[120px]">
              <p class="text-gray-700 font-semibold text-center">{partner}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Call to Action -->
    <section class="py-20 bg-gradient-to-br from-purple-900 to-purple-600">
      <div class="max-w-4xl mx-auto px-4 text-center">
        <h2 class="text-4xl md:text-5xl font-bold text-white mb-6">
          Ready to Make a Difference?
        </h2>
        <p class="text-xl text-white/90 mb-10">
          Whether you adopt, foster, volunteer, or donate, every action helps us save more lives.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a href="/adopt" class="bg-yellow-400 text-purple-900 font-bold py-4 px-8 rounded-full text-lg hover:bg-yellow-300 transform hover:scale-105 transition-all shadow-xl">
            Adopt a Pet
          </a>
          <a href="/get-involved/foster-care" class="bg-white text-purple-700 font-bold py-4 px-8 rounded-full text-lg hover:bg-gray-100 transform hover:scale-105 transition-all shadow-xl">
            Foster Care
          </a>
          <a href="https://donate.homelesshounds.com.au" target="_blank" class="bg-purple-800/50 backdrop-blur text-white font-bold py-4 px-8 rounded-full text-lg hover:bg-purple-800/70 transform hover:scale-105 transition-all shadow-xl border border-white/20">
            Donate Now
          </a>
        </div>
      </div>
    </section>
  </main>
</Base>